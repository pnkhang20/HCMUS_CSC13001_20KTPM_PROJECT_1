<Window x:Class="Management.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Management.Views"
        mc:Ignorable="d"
        Title="LoginView" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Login" FontSize="24" HorizontalAlignment="Center" Margin="0,10,0,20"/>

        <StackPanel Grid.Row="1" Margin="30">
            <TextBlock Text="Email:"/>
            <TextBox x:Name="EmailTextBox" Margin="0,5" VerticalAlignment="Center" HorizontalAlignment="Stretch"/>

            <TextBlock Text="Password:" Margin="0,15,0,5"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,5" VerticalAlignment="Center" HorizontalAlignment="Stretch"/>

            <Button x:Name="LoginButton" Content="Login" Margin="0,20,0,0" HorizontalAlignment="Stretch" Click="LoginButton_Click"/>
        </StackPanel>

        <TextBlock x:Name="ErrorMessageTextBlock" Grid.Row="2" Foreground="Red" Margin="30,10,30,0"/>

    </Grid>
</Window>
